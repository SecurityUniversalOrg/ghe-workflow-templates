name: IaC • Plan/Apply • AWS
on:
  workflow_dispatch:
permissions:
  id-token: write
  contents: read
jobs:
  plan-apply:
    uses: SecurityUniversalOrg/platform-arc-infra/.github/workflows/plan-apply-aws.yaml@main
